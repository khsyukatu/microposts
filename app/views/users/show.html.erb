<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>
<p>
  <% if logged_in? %>
    <% if current_user == @user %>
      <%= link_to "編集する", edit_user_path(@user) %>
    <% end %>
  <% end %>
</p>